/*
var devTime
fetch(window.location.href + 'time').then((res) => {
    console.log(res.text())
	devTime = res.text()
})

setInterval(() => {
	document.getElementById('time').innerText = new Date()
		.toTimeString()
		.split(' ')[0]
}, 100)
*/